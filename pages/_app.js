import '../styles/globals.css';

function MyApp({ Component, pageProps }) {
  // Global Error Handling, Loading States, etc can be added here
  return <Component {...pageProps} />;
}

export default MyApp;